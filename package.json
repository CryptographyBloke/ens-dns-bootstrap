{
  "name": "ens-dns-bootstrap",
  "version": "0.1.0",
  "description": "One-command Cloudflare bootstrap for importing a DNS domain into ENS onchain.",
  "type": "module",
  "bin": {
    "ens-dns-bootstrap": "./src/cli.mjs"
  },
  "scripts": {
    "test": "node --test",
    "start": "node ./src/cli.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "keywords": ["ens", "dnssec", "cloudflare", "ethereum", "web3"]
}